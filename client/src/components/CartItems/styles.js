import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  container: {
    padding: "1rem",
    display: "flex",
    flexDirection: "column",
  },
  item: {
    margin: "auto",
    padding: "auto",
  },
  grid: {
    maxWidth: "800px",
    width: "auto",
  },
}));
